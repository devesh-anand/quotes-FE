import React from "react";

const FormButton = () => {
   return (
      <div>
         {/* The button to open modal */}
         <label htmlFor="form-modal" className="btn text-white">
            Add Quote
         </label>
      </div>
   );
};

export default FormButton;
